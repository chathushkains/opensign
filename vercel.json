{
  "version": 2,
  "buildCommand": "cd apps/OpenSign && npm install && npm run build",
  "outputDirectory": "apps/OpenSign/build",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
